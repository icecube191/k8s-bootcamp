apiVersion: v1
kind: Service
metadata:
  name: postgres-service       # What the FastAPI app connects to
  namespace: bootcamp2
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP              # Internal only - database never exposed externally
