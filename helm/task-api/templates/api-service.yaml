apiVersion: v1
kind: Service
metadata:
  name: task-api-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: task-api
  ports:
    - port: 80
      targetPort: {{ .Values.api.port }}
  type: ClusterIP
